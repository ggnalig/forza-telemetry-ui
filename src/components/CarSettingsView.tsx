import React from "react";

// Placeholder for Phase C - full tree (Car -> Tune) + General settings tab
// with SliderInput-based override editing lands next. Exists now just so
// App.tsx's sidebar shell (Phase B) compiles and is navigable end to end.
export const CarSettingsView: React.FC = () => {
  return (
    <div className="min-h-screen bg-black flex items-center justify-center text-gray-500 text-sm">
      Car Settings - coming in Phase C
    </div>
  );
};
